# 發布

線上履歷是 `index.html`，透過 GitHub Pages 發布於
<https://gabrielchen0314.github.io/Portfolio/>。

## 怎麼更新

```
1. 改 index.html
2. git commit && git push
```

推上 `main` 之後 Pages 會自行重建，沒有第二個步驟。

## 唯一來源原則

`index.html` 是**唯一的來源**，發布出去的頁面是它的產物，不是反過來。
不從線上頁面反推內容。

2026-08-10 之前這份 HTML 只存在於暫存目錄，session 清掉就沒了——而它是唯一的來源。
同型的狀況先前踩過一次：東西只存在於發布平台，每次要改都得先把整份抓回來原地重寫，
沒有 diff、改壞無法回溯。處置就是這一條：**來源檔進版控，發布是單向的。**

## 一段歷史

2026-08-10 到 08-12 期間，這份履歷是以 claude.ai Artifact 發布的私有頁面。
改用 GitHub Pages 之後那條線廢止——**兩邊都留會內容分岔**，而分岔的代價是
讀者手上的連結停在哪一版無從得知。

那次留下一條仍然成立的教訓：Artifact 的更新規則是「同一對話 ＋ 同一檔案路徑
→ 更新同一網址」，任一項不同就會產生**第二個**網址，而先前分享出去的連結
永遠停在舊版本。凡是「發布出去就有人持有連結」的東西，都要先問清楚
它的同一性是靠什麼認的。
