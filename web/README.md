# web/ — 線上履歷的來源檔

> `index.html` 是**唯一的來源**。發布出去的頁面是它的產物，不是反過來。

---

## 發布網址

```
https://claude.ai/code/artifact/REDACTED
```

目前為**私有**，只有本人看得到。要給別人看必須從該頁面的分享選單明確分享。

> ⚠️ 若此 repo 日後轉為 public，把這行網址移到一個 gitignore 的檔案裡再公開——
> 這個頁面含真實姓名與 email。

---

## 怎麼更新

```
1. 改 index.html
2. git commit
3. 用 Artifact 工具發布，file_path 指向本檔案
```

### 🔴 換機器 / 換對話時必須帶 url 參數

Artifact 的規則是「**同一個對話 + 同一個檔案路徑** → 更新同一個網址」。

因此在**新的對話**或**另一台機器**上發布時，**必須把上面那個網址當 `url` 參數傳進去**，
否則會產生一個全新的網址，而履歷上那個連結會停在舊版本。

```
Artifact(file_path="web/index.html", url="https://claude.ai/code/artifact/REDACTED")
```

---

## 為什麼要有這個檔案

2026-08-10 之前，這份 HTML **只存在於暫存目錄**（`AppData\Local\Temp\...\scratchpad\`），
session 清掉就沒了，而它是唯一的來源。

這與先前教材那次的狀況同型：「只存在於發布平台，沒有本機來源檔——
每次要改都得先把整份抓回來原地重寫，沒有 diff、改壞無法回溯」。

**處置相同**：來源檔進版控，發布是單向的（原始碼 → 網頁），不從網頁反推。

---

## 內容與 repo 其他部分的關係

| 這裡 | 對應 |
|---|---|
| 履歷區塊（姓名／能力／經歷） | 只在這裡，repo 其他地方沒有 |
| 六個專案的摘要與「最值得講的一個」 | 完整版在 [`../projects/`](../projects/) |
| 知識體系的 12 張卡 | **只是摘要**；完整內容不在本 repo（見根目錄 README 說明） |

改動專案內容時，**兩邊都要改**——這正是 `projects/04` 裡記的那個教訓：
「同一份文件兩處相反，讀到哪一處就得到哪個結論」。
